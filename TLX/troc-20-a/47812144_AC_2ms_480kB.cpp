#include <bits/stdc++.h>
using namespace std;
int main() {
#ifdef memset0
  freopen("A.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  int x, y;
  cin >> x >> y;
  cout << (x > y ? "YES" : "NO") << endl;
}